# Platform documentation

The platform lane covers native desktop helpers and generic bridge utilities.

- [`native-bridge.md`](native-bridge.md) — renderer/Rust platform boundaries,
  shared Rust helpers, and stable command wrapper policy.

The platform lane must stay free of Hermes dashboard routes, Beszel collection
paths, and future service-specific APIs.
